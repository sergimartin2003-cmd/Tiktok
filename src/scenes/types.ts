// =============================================================================
// scenes/types.ts — Contrato común de todas las escenas.
// =============================================================================

/** Props que recibe toda escena: su propia duración, para el camera push. */
export type SceneProps = {
  durationInFrames: number;
};
